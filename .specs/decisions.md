# Decision Log

The authoritative status of every design decision. Specs reference these IDs inline.

Statuses: **Final** (decided; changing it requires a superseding entry) ·
**Pending** (alternatives + recommendation documented; not confirmed by the product
owner) · **Superseded**.

Rationales live in the linked specs; this log is the index and the status of record.
When a Pending decision is confirmed, update its status here **and** remove the Pending
labels at its spec references, in the same change.

## Final decisions

From the product brief (2026-07-10):

| ID | Decision | Spec |
|---|---|---|
| D-001 | Own-credentials model: every user creates their own Google Cloud project + Desktop OAuth client; Chronicle ships zero credentials | `authentication.md` |
| D-002 | Subscriptions-only chronological feed; no recommendations/trending/algorithmic feeds — the full "Never" list | `non-goals.md`, `feed.md` |
| D-003 | Video states (read/ignored/favorite/watch-later) are **Chronicle concepts, local-only, never synced to YouTube**. (Narrowed by D-032 on 2026-07-10: the original "readonly scope only" clause was superseded — see D-032. The local-state rule itself is untouched.) | `feed.md`, `authentication.md` |
| D-004 | Secrets in OS secure storage (libsecret/Keychain/Credential Manager); never uploaded anywhere | `authentication.md` |
| D-011 | Local-first & privacy-first: no Chronicle servers, no telemetry, no analytics, ever | `non-goals.md`, `local-data.md` |
| — | Desktop-first; minimal RSS-reader-style UI; dark mode first; keyboard shortcuts; backend owns OAuth/API, frontend owns UI; clean architecture with replaceable parts | `ui.md`, `architecture.md` |
| — | MVP scope = the features in `features.md` §MVP; comments optional and excluded from MVP | `features.md` |

Confirmed by the product owner (2026-07-10, decision session):

| ID | Decision | Spec |
|---|---|---|
| D-006 | Playback = **embedded YouTube IFrame player**, styled as clean as possible (minimal branding, Chronicle end-overlay suppressing related videos); per-video "open in browser" escape. Building our own player over extracted streams was **rejected**: TOS violation, breaks on YouTube internal changes, and deprives creators of ad revenue (views without monetization). mpv/yt-dlp path demoted to Future idea. | `playback.md` |
| D-005 | Desktop shell = **Electron** (follows from D-006: Chromium guarantees embed playback reliability on all three OSes; Linux system webviews do not) | `architecture.md` |
| D-007 | Feed data source = **Hybrid**: sync subscriptions locally → RSS feeds of those channels for discovery (free) → batched `videos.list` hydration for metadata. Only recent videos fetched by default; older content loaded on demand. | `youtube-api.md` |
| D-012 | Testing-mode 7-day token expiry: wizard **explains and recommends** publishing the app to production but it is the **user's optional choice**. On every app open, Chronicle validates the connection; on `invalid_grant` it prompts re-auth via the **default browser** (low friction — the user is typically already signed in to Google and just confirms). | `authentication.md`, `onboarding.md` |
| D-010 | Video state model = **read-status enum (`unread`/`read`/`ignored`) + orthogonal `favorite` and `watch_later` flags**, because a video can be read AND favorite AND queued simultaneously (email-style: read state vs. star). | `feed.md`, `local-data.md` |
| D-027 | Older-content pagination = **continuous scroll** through the user's own local chronological archive (with on-demand deeper backfill per channel). This is bounded, mechanical scrolling through content the user chose — distinct from, and not a violation of, the ban on algorithmic infinite feeds. New content at the top remains finite ("caught up" state unchanged). | `feed.md`, `non-goals.md` |
| D-028 | ~~Shorts are never displayed. Not now, not ever. No toggle.~~ **Superseded by D-035** (2026-07-12, B-028). Detection heuristic (duration + `/shorts/` URL check) is unchanged and still specced in `feed.md`. | `feed.md`, `non-goals.md`, `features.md` |
| D-029 | **Universal video opening**: the player view opens **any** YouTube video, subscribed or not — YouTube links in video descriptions open in-app; a video can be opened by pasted URL. The recommendation ban targets *algorithmic selection*, not user-initiated navigation or creator curation. Externally opened videos never enter the feed and never count as unread; local states (favorite/watch-later) work on them. | `playback.md`, `feed.md`, `non-goals.md` |
| D-031 | **YouTube search, working like YouTube's search**: the user types a query and finds videos and channels across all of YouTube, including channels they don't follow; results open in Chronicle's player/channel view. It is a *deliberate tool* — inert until the user types; results are never injected into the feed or any other view. Costs: pasted @handle/URL → `channels.list`/`videos.list` (1 unit); free-text → `search.list` (100 units/query, ~100/day headroom). **Implemented 2026-07-12 (B-009):** a "Mine" / "YouTube" scope toggle next to the existing local `/` filter; free-text search only fires on Enter (never per keystroke), rendering a flat video+channel results list distinct from the feed. Pasted @handle/URL cheap path is a separate, pre-existing surface (`UrlPrompt.tsx`, D-029) — untouched by this change. **Revised same-day (`bugs.md` B-054):** the scope toggle is gone — the `/` field has exactly one behavior now, searching YouTube directly on Enter. It no longer re-filters the locally loaded feed at all (that live-filter-while-typing behavior was the actual source of the reported confusion, not just the toggle) — local caching stays invisible to the user, browsing already-followed channels is the sidebar's job. | `non-goals.md`, `features.md`, `youtube-api.md` |
| D-032 | **Scope policy: minimal at start, incremental by feature.** Initial auth requests `youtube.readonly` only. YouTube-side interaction features — subscribe, **like, comment** — are planned (not banned), each unlocking via incremental authorization (`youtube.force-ssl`) the first time the user invokes it. Chronicle-side local states still never sync (D-003). What remains banned is Chronicle-generated engagement *mechanics*, not the user expressing themselves on YouTube. **Implemented 2026-07-12 ([[B-006]]):** like a video (`videos.rate`/`.getRating`), read a video's comment thread (`commentThreads.list`, no write scope needed), post a top-level comment (`commentThreads.insert`) and reply to one (`comments.insert`). **API gap found while implementing, not a product choice:** the public YouTube Data API v3 has no endpoint to like a *comment* — only videos. Comment `likeCount` is shown read-only; there is no like button on a comment. **Revised 2026-07-13 (`bugs.md` B-067):** the first four incremental-consent call sites (like, subscribe, post comment, reply) originally opened the system browser for Google's consent screen the instant a write action was attempted, with no in-app warning — jarring, since the user had no reason to expect their click to pop open a browser tab. Each of those IPC handlers now returns a `write-scope-required` result instead of silently triggering the flow; the renderer shows an explanatory confirm dialog first (`useWriteScopeGate`), and only calls the new dedicated `requestWriteScope` IPC method (which does what those handlers used to do inline) once the user confirms. Unsubscribe's own incremental-consent call site (per-account, not the shared primary-account `authFlow`) was **not** converted — same gap remains there, noted as a known limitation in `bugs.md`. **Revised again 2026-07-13 (`bugs.md` [[B-081]]):** unsubscribe's gap is closed via a channel-scoped `requestWriteScopeForChannel(channelId)` IPC (resolves the owning account instead of assuming the primary one) and an optional second argument on `useWriteScopeGate.run()` for callers that need a non-default consent request. Separately: reading comments (`commentThreads.list`) turns out to also require the write scope in practice, not just readonly as this decision originally assumed — confirmed by the owner's live testing (granting the scope via Like is what makes comment-reading start working). Gated the same proactive way as every other write action rather than carved out as a special case. | `authentication.md`, `non-goals.md`, `features.md` |
| D-030 | Channel view offers **both follow mechanisms**: (a) "Subscribe on YouTube" (`subscriptions.insert`, 50 units, incremental scope per D-032) and (b) "Follow locally" via RSS — Chronicle-only, invisible to the user's YouTube account. **Implemented 2026-07-12 (B-009):** mechanism (a) only, reachable from a YouTube search result's Subscribe button — subscribing runs the same incremental write-scope consent as [[B-010]]'s unsubscribe, then a channel-scoped sync to populate its videos. Mechanism (b), "Follow locally," is **not yet built** — that's D-033's accountless-mode work, still future. | `features.md`, `vision.md` |
| D-033 | **Accountless mode** (enabled by local follows): Chronicle is usable **without any Google/YouTube account** — follow channels locally via RSS, chronological feed, embedded playback, local states. The onboarding wizard becomes an optional path that unlocks account features (subscription import, search, like/comment, full metadata). Degraded-mode details (no `videos.list` hydration → no duration badges; Shorts detection via HEAD-check on every new video instead of duration-filtered candidates) specced in `features.md`. Lands together with D-030 (post-MVP #1); MVP still assumes the authenticated path. | `features.md`, `onboarding.md`, `authentication.md` |
| D-009 | Frontend framework = **React + TypeScript** (confirmed) | `architecture.md` |
| D-018 | Show view counts on feed rows = **on by default**, with a Settings toggle to hide. Supersedes the "hidden by default" choice exercised in M5 — the product owner reported the default as friction (B-029): the data costs no extra quota (captured at hydration) and is normal context on a video row. | `feed.md` |
| D-023 | Muted-thumbnail treatment = **rejected; thumbnails render at normal (full) opacity always**. The M3 prototype (dimmed at rest, full opacity on hover/selection) was reported as an unwanted default (B-034) and dropped rather than kept. | `ui.md` |
| D-035 | **Shorts are shown in the feed, tagged with a badge, with a "Show Shorts" Settings toggle (default on) to hide them.** Supersedes D-028's unconditional exclusion (2026-07-12, B-028) — the product owner argued unconditional exclusion contradicted the app's own "agency, not austerity" test: the feed only shows content from channels the user chose to follow, so filtering some of it without consent is the algorithm's will in miniature. Detection pipeline (duration candidate + `/shorts/{id}` HEAD confirmation) is untouched; only the display policy reversed. | `feed.md`, `non-goals.md`, `features.md` |
| D-037 | **List vs. grid feed layout, plus a shared file-explorer-style item-size control (xs/small/medium/large/xl) — both persisted, both inline in the feed topbar, not in Settings.** Supersedes the "one column; no masonry/grid" clause of the original Layout decision (2026-07-12, B-007) — the product owner argued a thumbnail grid is a personal layout preference, not an engagement mechanic, so the "who is driving?" test does not ban it. Also supersedes D-022's two-step "density" (comfortable/compact): `itemSize` replaces it with steps that scale *both* list rows and grid cards from the same slider, since a size preference applies to either layout the same way. **Two rounds of same-day owner feedback:** first pass put the layout choice in Settings → Appearance (mirroring the old density select) — the owner wanted it in the listing itself, so it moved to an icon toggle (`⊞`/`☰`) in the topbar and the Settings dropdown was removed; second pass added the item-size slider using the same inline placement from the start, once the file-explorer-style pattern was requested. **2026-07-12 follow-up (product-owner request):** three steps (small/medium/large) widened to five (`xs/small/medium/large/xl`) for finer granularity, with the xl step a deliberately bigger jump than the other, roughly-uniform steps — it's the "as big as it gets" ceiling, not another even increment. Same change fixed a CSS specificity bug where the list-row `.thumb` width overrides (`.feed-scroll.size-X .thumb`) also matched the grid card's thumbnail (`.card-thumb-wrap .thumb`, meant to always be `width: 100%`), so grid thumbnails at non-medium sizes rendered as a small fixed-width image adrift in a wider card instead of filling it; the row overrides are now scoped to `.row .thumb` so they cannot leak into the grid. Also added `cursor: pointer` on list rows and grid cards (both open the video on click), with `cursor: default` kept on the non-clickable "ignored, undo" strip. List rows, virtualization, and all per-video actions are unchanged and shared between both layouts (`FeedList.tsx`). Grid column count is derived from the feed container's width (`ResizeObserver`, min card width scales with item size), not hardcoded. | `ui.md` |
| D-034 | **M0 toolchain** (2026-07-11): **npm** as package manager (explicit product-owner choice — no pnpm), **electron-vite** (dev/build for main+preload+renderer), **node:sqlite** (Node's built-in synchronous SQLite driver), **Vitest** (unit tests), **ESLint + Prettier**, **dependency-cruiser** enforcing the architecture.md layer rules as lint errors. (Amended same day by product owner: standalone GitHub Actions CI removed from M0 — a single smarter pipeline with releases + automatic binary builds will be designed later, see `roadmap.md` M5. Amended in M1: better-sqlite3 replaced by node:sqlite — no native module, no Electron ABI rebuild, and storage contract tests run under the system Node in Vitest; stable in Electron's Node 24, experimental-but-functional in system Node 22.) All replaceable without touching `core/`/`ipc/`. | `architecture.md`, `roadmap.md` |
| D-038 | **Default playback speed**, a Settings → Playback dropdown over the YouTube IFrame API's fixed rate set (0.25×–2×, default 1× "Normal"). The player opens already set to this rate — issued once the embed announces itself and re-issued on the first `onStateChange(playing)` event, mirroring D-006/B-038's quality-reissue pattern, since YouTube can reset a requested rate the moment the stream actually starts. Per-video changes made from the embed's own speed control are session-only and never write back to the setting (product-owner request, 2026-07-12). | `playback.md` |
| D-040 | **Multi-account schema (B-003):** a channel followed by more than one local account splits into shared/deduped channel facts (`channels`) + an `account_channels` junction table for the per-account relationship (subscribed/favorite/subscription id/backfill cursor). **Exercised 2026-07-12:** a simpler single `account_id` column added directly to `channels` was considered and rejected mid-implementation — it would let one account's re-subscribe silently overwrite another's row for the same channel, a correctness bug for a feature specifically about supporting more than one account. The junction table is the only one of the two that's correct when two accounts follow the same channel. | `local-data.md` |
| D-041 | **Multi-account scope simplifications**, exercised alongside D-040, all deliberate and narrower than a maximal implementation would be: **(a)** Settings' Connection section and the first-run wizard keep managing one "primary" account only — completely unchanged UX for existing single-account users; all additional accounts are managed through a new sidebar Accounts section instead. **(b)** The primary account cannot be removed via the Accounts section (only Settings' existing Sign Out) — the Remove option is disabled with an explanatory tooltip for the primary account (fixed 2026-07-12, `bugs.md` [[B-066]]; previously failed silently). **(c)** Subscribing to a new channel found via search ([[B-009]]) always subscribes under the primary account — no account picker; an action on a channel followed by more than one account applies to whichever account owns it first if more than one does (rare: two of *your own* accounts both following the identical channel). | `local-data.md`, `authentication.md`, `ui.md` |
| D-039 | **Favorited-channel priority section (B-042):** duplicate placement — a favorited channel's unread video appears in **both** the priority section and its normal chronological bucket, never removed from the bucket. **Exercised 2026-07-12:** consistent with D-010's orthogonal-flags model, where favorite/watch-later never remove a video from its normal spot — a video being prioritized shouldn't make "did I see everything from Tuesday" miss it from Tuesday's bucket. The priority section only lists unread videos (parallel to the unread view), capped at 20. Still rough per `bugs.md` [[B-063]]: the section doesn't yet follow the feed's layout/item-size settings. | `feed.md` |
| D-042 | **Archive-backfilled videos default to read, not unread (`bugs.md` B-058):** a channel's deep back-catalog (`SyncService.backfillArchive`, D-027/B-002) predates the user actively following/using Chronicle — it was never "missed" chronologically, so surfacing it shouldn't inflate unread counts app-wide. **Exercised 2026-07-12:** `SyncRepository.markVideosReadIfUnset` inserts a `video_state` row with `read_status = 'read'` for every newly archive-backfilled video, `ON CONFLICT DO NOTHING` so it never overwrites a real state row from another path (e.g. hydrate-on-open via search). Deliberately **not** applied to routine sync's gap-backfill (`backfillGap`) — those videos are genuinely missed uploads since the last sync, not archive history, so they stay unread as before. | `feed.md` |
| D-024 | **Linux packaging = AppImage only for the first release.** Supersedes the original "Flatpak primary + AppImage" recommendation — the product owner deferred Flatpak's extra manifest-maintenance/store-submission overhead until there's demand for it. AppImage is a single portable binary with zero extra publishing infrastructure, built straight from `electron-builder`. Flatpak stays a Future idea; revisit post-first-release. **Exercised 2026-07-13.** | `roadmap.md` |
| D-025 | **Windows builds ship unsigned.** The NSIS installer is not code-signed; Windows SmartScreen shows its "Windows protected your PC" warning on first run, with a documented bypass (More info → Run anyway) in the README. **Exercised 2026-07-13.** | `roadmap.md` |
| D-043 | **macOS builds ship unsigned, not notarized.** The dmg is not signed or notarized; Gatekeeper blocks the unidentified app by default, with a documented bypass (right-click → Open, or System Settings → Privacy & Security → Open Anyway) in the README. This wasn't previously a tracked Pending decision — `roadmap.md`'s M6 bullet assumed a "notarized dmg" as fact; this entry corrects that assumption to match what's actually shipping. **Exercised 2026-07-13.** | `roadmap.md` |
| D-026 | **Update mechanism = automatic background check against GitHub's public Releases API** (`GET /repos/AdrianoMoura/ChroniclePlayer/releases/latest`), not a hand-rolled signed static manifest as originally recommended. The privacy invariant this decision protects — no identifiers or telemetry sent — is satisfied by an unauthenticated GET that only asks "what's the latest tag"; a custom manifest would need its own signing/publishing infrastructure for no additional privacy benefit, and the API's `tag_name` already *is* the version, so there's nothing extra to keep in sync. Checks once at startup and every 24h from a main-process timer, compares semver to `app.getVersion()`; on a newer version, shows a dismissible in-app banner linking to the release page — **never** auto-downloads or auto-installs (would fail the "who's driving?" test). Off-switch in Settings, default on. **Exercised 2026-07-13.** | `roadmap.md` |
| D-013 | **Secret-store fallback when no OS keychain = Electron `safeStorage`, else machine-derived key + honest warning** (rejected: refuse to persist). **Exercised in M2 (2026-07-11):** safeStorage when a real keychain backs it; else AES-256-GCM under a scrypt key from `/etc/machine-id`+user, `isSecure()=false` → UI warning. The writing cipher is pinned in the store file. Real-world smoke (product owner, keyring-less niri session) hit the missing-fallback crash on day one — `safeStorage.encryptString` throws without a keychain — confirming the fallback is mandatory, not theoretical. | `authentication.md` |
| D-016 | **Background refresh default interval = 30 min, configurable 15 min–manual** (rejected: fixed 15 min or hourly). **Exercised:** 30-min default (M2); configurable 15/30/60/manual in Settings (M5, 2026-07-11). | `youtube-api.md` |
| D-014 | **Wizard validation for Google-console steps 1–5 = checkbox confirmations + failure→step mapping at connect time** (rejected: attempt live validation). **Exercised in M4 (2026-07-11).** | `onboarding.md` |
| D-015 | **Wizard media = static annotated screenshots** (rejected: per-step recordings). **Exercised in M4 (2026-07-11):** asset pipeline with verified-on dating built. Capture of the actual screenshots is still open, tracked as an M6 exit item in `roadmap.md` — the decision (format) is settled, only the asset-capture work remains. | `onboarding.md` |
| D-044 | **Wizard Step 8 ("First sync") removed as a blocking step** (`bugs.md` [[B-090]]) — supersedes the original design where the wizard waited for the first sync to finish before unlocking "Open feed." Step 7's successful connection already triggers a first sync in the backend (the same `connectGoogle` path used ever since M2); the wizard step was a second, redundant trigger for that same sync, gating on an event stream the not-yet-mounted step could easily miss (root cause of the button getting stuck disabled). **Exercised 2026-07-15**, per the product owner's own stated preference: the wizard now finishes the instant Step 7 connects, landing on the feed screen with sync continuing in the background exactly like every later refresh already works. | `onboarding.md` |
| D-045 | **Embedded-player session = Electron's ordinary unnamed default session (no custom `webPreferences.partition` anywhere), shared by the main window (and therefore the player's plain `<iframe>`, which always inherits its embedding page's session) and a new "Sign in to YouTube" action** (`bugs.md` [[B-093]]) — chosen over per-cookie injection between two separate sessions. Chronicle's own OAuth connect flow (D-001/D-012) always ran in the *system* browser, never touching Electron's own browsing session at all, which is why the embedded player's session started out never authenticated to Google — the previously-found workaround (clicking the iframe's own "Watch on YouTube" corner link, which opens a same-session popup window by default Electron behavior, signing in there) already proved a *shared* session is sufficient once actually signed into; the gap was only that this path had no deliberate, discoverable trigger. Settings → Connection gained a plain "Sign in to YouTube" button that opens a window at youtube.com in that same default session — no automation, no scraping, no cookie extraction (`youtube-api.md` §Terms-of-service constraints): the user signs in themselves, exactly like the corner-icon workaround, just reachable on purpose. **Revised same day**: first implemented as an explicitly *named* persistent partition instead of the default session, on the theory that naming it made the sharing more intentional/documented — this broke every thumbnail in the app, since `protocol.handle()` (`thumb://`) registers on `session.defaultSession` specifically and a custom partition is a different session with no handler on it. Reverted to the default session everywhere; no functional loss, since the default session was already what every window/iframe here implicitly shared before B-093 touched any of this. **Exercised 2026-07-15**, per the product owner's direction; needs the owner's own live validation (open the sign-in window, confirm authenticated state persists into the player) before considering the underlying bot-check symptom actually resolved. | `authentication.md` |
| D-046 | **Miniplayer continuity (`bugs.md` [[B-045]]): docking to the corner keeps the exact same live `<iframe>` instance mounted (no restart); extracting to an always-on-top window cannot, and hands off a playback snapshot to a fresh instance there instead.** Two different answers for what looked like one question, because they're not the same technical problem. Extracting opens a second `BrowserWindow` (`alwaysOnTop: true`) — a genuinely different renderer process, so the DOM node itself cannot move there — loading the exact same renderer bundle with an `?extract=<videoId>&t=<seconds>&autoplay=<0|1>` query string that `main.tsx` checks to render a bare, chrome-less `ExtractedPlayerWindow` instead of the full app; the main window's player then closes outright, since playback now lives in that window. Docking stays within the same renderer process, so the same live iframe *should* be able to keep running — **first attempt got this wrong**: it used a React portal to move the iframe between a full-view slot and a corner-box slot, which shipped and immediately restarted the video on every dock in the owner's live test, because moving an `<iframe>` to a different DOM parent (exactly what a portal does, even without detaching it from the document) makes Chromium reload it — a browser behavior, not a React bug, not fixable by portaling "more carefully." **Revised same day**: dropped the portal; `PlayerSurface` now renders at one permanently stable tree position and never moves in the DOM at all, instead measuring whichever slot placeholder (`ResizeObserver`-watched, in a non-scrolling region so scroll ticks don't need tracking) the active layout (`PlayerDetails` or `MiniPlayerBar`, both still always-mounted, CSS-hidden when inactive) provides, and mirroring that rect via `position: fixed` inline styles. Also added, per the owner's same live-test feedback: the corner box is resizable (native `resize: horizontal`, default width raised from 260px to 360px) and the explicit "Miniplayer" button was removed — the owner's actual ask was automatic-only docking on leaving a playing video, and the manual button read as redundant clutter once that automatic path worked. Per the product owner: both docking and extraction legs landed in the same pass, accepting the reload for the extract leg as an inherent limitation of crossing a process boundary, not something to keep chasing a workaround for. **Revised again same day, third round**: the next live test found docking didn't fire via *any* path — root causes were (a) every "hard" navigation-away action (sidebar clicks, search submit, account switch) called `setPlayerStack([])` directly, never consulting the dock decision at all, fixed with a shared `leavePlayerForNavigation()` that collapses the stack to the current video and docks it if still going; and (b) the dock decision itself was gated on the IFrame API's literal `PLAYING` state, which depends on an `onStateChange` round trip not reliable enough to gate a core feature on — loosened to "still going" (true unless explicitly paused or ended), exposed as `PlayerSurfaceHandle.isStillGoing()`. **Revised again same day, fourth round**: the next live test surfaced six more gaps, all fixed in this pass. Extraction gained a return path — closing the always-on-top window now broadcasts `player:restoreFromExtract`, which re-opens that video docked in the main window — and reliable autoplay, by having `ExtractedPlayerWindow` speak the same postMessage widget protocol as the main player (`enablejsapi=1`, explicit `command('playVideo')` after handshake, `autoplayPolicy: 'no-user-gesture-required'`) instead of trusting the embed's own `autoplay=1` alone under Electron's stricter top-level-navigation policy; the same protocol's `infoDelivery` messages are what let the restore resume close to the actual playback position (persisted via `beforeunload` → `setResumePosition`). The extract window (and, once noticed, the B-093 sign-in window) now also calls `removeMenu()` — neither should show Electron's native menu bar. The resize handle moved from native CSS `resize: horizontal` (which anchors its own grab handle to the box's bottom-right corner — exactly where the whole box already sits against the screen's corner, making it awkward to grab) to a custom top-left drag handle, with the resulting width persisted as `miniplayerWidth` in settings rather than resetting every launch. And a real navigation-stack bug was fixed: opening a video from any *fresh browsing* action (feed, search, priority/channel-preview sections, URL prompt) now explicitly replaces the stack instead of defaulting to push mode, so docking one video and then opening an unrelated one no longer buries the first one for Back to surface later — push mode is now reserved for the one case it's actually meant for, opening a video via an in-description link while watching another. **Revised again same day, fifth round**: the fourth round's own resize-handle and extract-autoplay fixes turned out not to work either, both for reasons specific to how those fixes were built rather than the underlying ideas being wrong. The resize handle was positioned `absolute; top: 0; left: 0` *inside* `.miniplayer` (z-index 20) — but the video itself is a separate always-mounted element (`.player-stage`) mirroring the stage slot's rect at z-index 21, one layer above, painted directly over that same corner: no z-index tweak on the handle could win, since a child can never paint above a sibling stacking context with a higher z-index than its own parent's. Fixed by giving the handle real layout instead — `.miniplayer` is now a flex row of a fixed-width left-edge strip plus a `.miniplayer-content` column holding the video slot and title bar, so the mirrored iframe (which only ever covers the stage slot's own rect, now inset by the strip) structurally cannot overlap the handle. Separately, the extract window's autoplay fix from last round was correct but never actually exercised: `extractToWindow` hands the extract window a `playing` flag read from `PlayerSurface`'s `getPlaybackSnapshot()`, which used the exact same overly strict `playerStateRef.current === 1` check that round three of this same decision already found and loosened for the *docking* decision (`isStillGoing()`) — `getPlaybackSnapshot` had simply never been updated to match, so it kept handing off `playing: false` whenever the autoplay-initiated `onStateChange` round trip hadn't landed yet, and the extract window's autoplay logic was faithfully honoring that (correct) instruction. Fixed by having `getPlaybackSnapshot().playing` reuse `isStillGoing()` too. **Revised again same day, sixth round**: the owner found D-038's default playback speed wasn't reaching the extracted window at all — it always opened at 1x. Root cause: `extractPlayer` never had a `defaultPlaybackRate` parameter in its IPC signature, so there was no path for the setting to travel through at all (unlike the main player, which gets it as a normal prop from `App.tsx`'s `settings` state) — not a bug in the widget-protocol plumbing itself, just a parameter that was never added when `extractPlayer` was first written. Fixed by threading it through every layer (`ChronicleApi.extractPlayer` → `preload.ts` → the `main.ts` IPC handler, validated against `PLAYBACK_RATES` server-side → `createExtractWindow` → the `?extract=` query string's new `rate=` param → `ExtractedPlayerWindow`), which applies it exactly the way `PlayerSurface` applies D-038: `setPlaybackRate` on the widget-protocol handshake, reissued on the `onStateChange` transition to playing, since YouTube can reset it back to 1x once the stream actually starts. **Revised again same day, seventh round**: the owner found the full-view player screen no longer scrolled as one page — a deliberate trade-off from the first round (a non-scrolling topbar+stage flex row so the stage slot's rect never moved, meaning `PlayerSurface`'s `position: fixed` mirroring never had to re-measure on scroll) that, once actually lived with, wasn't wanted: the video should scroll away with the rest of the page like a normal page, not stay pinned above a separately-scrolling info panel. Fixed by reverting `.player-view` to one normally-flowing scrolling block and teaching `PlayerSurface`'s measurement effect to also re-measure on scroll (not just resize), throttled to one `requestAnimationFrame` per tick — using a `window`-level `scroll` listener with `capture: true`, since scroll events don't bubble but do fire during the capture phase on ancestors, letting one listener there catch `.player-view` scrolling without a direct reference to it. **Revised again same day, eighth round** (plus the product owner's own request to raise the miniplayer's max resizable width from 640px to 1024px, `MINIPLAYER_MAX_WIDTH`): the sixth round's extract-window default-speed fix still didn't work live. The wiring itself (Settings → IPC → query string → `ExtractedPlayerWindow`) was correct; the bug was in the reissue mechanism, which reused D-038's normal pattern of reissuing `setPlaybackRate` on the iframe's `onStateChange(playing)` message — but the third round of this same decision already established that the *autoplay-initiated* `onStateChange` isn't reliably observed at all, the exact same reason `isStillGoing()` and the fifth round's extract-autoplay fix both had to stop trusting a strict state check. The rate reissue had simply never been given the same treatment. Fixed by reissuing on every `infoDelivery` message instead — a steady heartbeat once the widget is genuinely up, independent of which `onStateChange` events happened to land — for the first 3 seconds after load (wall-clock time, not video position, since most extractions hand off mid-video). **Exercised 2026-07-15**; needs the owner's own live validation (dock via Back/Esc, dock via sidebar navigation, maximize, resize + persistence, extract, extract autoplay, extract at the configured default speed, close-extract-to-restore, no menu bar on the extract window, replace-vs-push across all open paths, full-view page scroll with the video scrolling smoothly, and the modal-vs-miniplayer stacking order — `.overlay-backdrop` was given `z-index: 30` specifically so help/consent dialogs stay on top of a docked miniplayer) before considering this closed. | `playback.md` |
| D-047 | **On an account's very first sync, every discovered video is marked read on arrival — not just the backlog published before today** (`bugs.md` [[B-105]]) — supersedes the original B-020/B-069 rule, which only auto-read backlog videos and left same-day videos unread. **Superseding rationale (the product owner's own call, 2026-07-16):** a first sync has no prior visit to judge "new since you were last here" against — every video it discovers, today's included, is an equally uninformed guess about relevance, so assuming any of it is unread-worthy is an unjustified assumption, not a real signal. Every subsequent sync (routine or backfill) is unaffected and keeps marking newly discovered videos unread as normal — this only changes the account's very first run. **Exercised 2026-07-16:** `SyncService.refresh`'s `firstSync` branch now calls `repo.markVideosReadIfUnset` on every video in each hydrated batch unconditionally, instead of filtering by `publishedAt` against a same-day cutoff (the `backlogCutoff`/`startOfToday` logic was removed outright, not just widened). | `feed.md` |
| D-048 | **Removed the "channel unavailable" state entirely — an RSS 404 is just an ordinary per-channel failure now (retried next cycle), never a permanent, un-retried "channel deleted" verdict** (`bugs.md` [[B-110]]) — supersedes `youtube-api.md`'s original "Channel deleted/terminated" failure-handling row (mark `unavailable`, stop polling, show in a settings list). That row was never an actual product decision — it was an assumption baked in during earlier development with no owner sign-off and no corresponding `decisions.md` entry, surfaced now because it caused a real, silent bug: a single transient RSS 404 (YouTube's RSS edge can 404 for reasons that have nothing to do with the channel actually being gone) permanently excluded a channel from `listSubscribedChannels` — and therefore from every future routine sync **and** from the on-demand scroll-triggered archive backfill, since both read from the same query — with no retry, and no self-healing path, since the very query that would have reset the flag on a later successful check never ran again once excluded. The promised "settings list" surfacing this state was also never built, so the whole thing was invisible: a channel just silently stopped updating forever. **Superseding rationale (the product owner's own framing, 2026-07-16):** Chronicle's experience should work like YouTube's own pagination/discovery — keep trying to fetch content until a result genuinely comes back empty, that's how you know you've reached the end; a single failed attempt is never proof of anything permanent, and RSS calls cost no quota, so there's no efficiency reason to ever stop asking. **Exercised 2026-07-16:** the `'channel-unavailable'` domain error kind, `channelUnavailable()`, `markChannelUnavailable()`, and the `available` column/filter (`channels.available`, migrated away in schema v8) are all removed outright rather than left dormant; `rss-client.ts`'s 404 branch now falls through to the same `internal(...)` failure every other non-2xx RSS response already threw, which `discoverChannel`'s existing generic per-channel failure handling (log + retry next cycle) already covered before this change — no new retry mechanism was needed, only removing the special case that pre-empted it. | `youtube-api.md` |
| D-050 | **Tray-resident mode, OS auto-start, and opt-in new-video notifications — three fully independent Settings toggles, all default off; none gates any other.** **(1) Auto-start** — launch Chronicle on OS login: Electron's `app.setLoginItemSettings()` (Windows Registry Run key / macOS LaunchAgent, both built-in); Electron has no Linux equivalent, so `src/platform/linux-autostart.ts` hand-writes/removes an XDG `~/.config/autostart/*.desktop` entry (plain `fs`, no native module, D-034-compliant). **(2) Run in background** — closing the window hides it to a tray icon (`src/platform/tray.ts`, Electron's cross-platform `Tray`) instead of quitting; the tray menu is Open Chronicle / Refresh now / Quit. This is what lets the existing 30-min sync timer (D-016) keep running with no window open. **(3) New-video notifications**, via Electron's cross-platform `Notification` API — **corrected during implementation (2026-07-16, the product owner's own catch):** the original spec-only pass wrongly UI-disabled this toggle unless (2) was on, reasoning that "the app isn't alive to notice anything" otherwise — false, since the app is already alive any time its window is open at all (that's the existing D-016 timer, untouched), regardless of "Run in background." (2) only extends *when* the process stays alive (surviving window-close); it was never a hard prerequisite for (3). Settings now shows an informational note near the toggle instead of disabling it. Never fires on an account's first sync (`SyncReport.firstSync`) — that's backlog, not something that happened while backgrounded (mirrors D-047's reasoning). Notification scope is one segmented choice, not three settings: **All channels** · **Favorites only** (reuses the existing favorite-channel flag, zero new schema) · **Custom** — realized as a **new per-channel `notify` flag** (`account_channels.notify`, schema v9) toggled from the sidebar channel's own "⋯" context menu exactly like Favorite/Unfavorite, per the product owner's standing preference for inline per-item controls over a Settings checklist — Settings only shows the scope picker plus a hint pointing at that menu. `SyncService.refresh()` (`src/core/sync-service.ts`) gained a free-information per-channel tally, `SyncReport.newVideosByChannel`, from the same per-channel discovery loop that already existed — core stays notification-agnostic; the scope/first-sync policy lives in `main.ts` where the feature actually is. **Rejected alternative:** an OS-level scheduled task (Task Scheduler/launchd/cron) launching Chronicle headlessly just to sync+notify, then exit — three separate per-OS scheduling implementations instead of reusing the sync machinery that already exists, for no real resource saving over just staying resident. **Verification risk (Assumption, not yet confirmed):** the product owner's hands-on verification is Linux-only (`roadmap.md`'s M0 exit criterion), so the Windows/macOS auto-start and notification paths — including whether D-043's unsigned/non-notarized macOS build degrades Notification Center registration — cannot be live-verified directly; every capability feature-detects and disables itself silently rather than throwing/crashing if unavailable on a given platform. **Revised same day, redesign round (owner's own written spec, 2026-07-16):** three bugs and one UX redesign, found the moment the owner actually tried tray-resident mode live. **Bugs:** (a) toggling "Run in background" on/off repeatedly appeared to leave multiple ghost tray icons behind, one of which never disappeared — root cause wasn't the toggle logic itself (which was already correctly gated on `tray === null`) but a missing `app.requestSingleInstanceLock()`: before this feature, closing the window always quit the process, so relaunching (e.g. restarting the dev server) never left a stale instance; now that the window can close without quitting, a second launch on top of an already-tray-resident first instance spawns a fully independent second process with its own tray icon, and toggling Settings in one instance can't touch the other's — fixed by adding the lock plus a `second-instance` handler that just re-shows the existing window. (b) A real, separate correctness bug found during the fix: `applyBackgroundMode`'s disable branch called `tray?.destroy()` then `tray = null` inside one `try` — if `destroy()` ever threw, the reset line never ran, leaving `tray` referencing a possibly-dead object and blocking any future recreation; pulled into its own `destroyTray()` helper with its own try/catch so the reference always clears. (c) Emoji removed everywhere per the owner's direction: the sidebar's old notify menu item (superseded anyway by the redesign below) and, flagged in the same pass despite predating D-050 entirely, the like button's 👍 (`player.action.like`/`liked`) — dropped to plain text, matching every sibling action label (subscribe/ignore/watch-later), which already carry no icon and rely on the existing `active`-state styling instead. **Redesign:** collapses the notification *scope* from three choices to two — **All Channels** (ignores the per-channel flag) and **Selected Channels** (respects it) — dropping "Favorites only" as a distinct scope entirely; a new global convenience toggle, **auto-enable notifications when favoriting**, syncs the per-channel `notify` flag to a channel's favorite state at the moment either is toggled (one-shot nudge, not a live binding — a later manual notify change on that channel is never re-overridden until the next favorite/unfavorite). Turning the convenience toggle *on* immediately bulk-applies notify=true to every current favorite, no confirmation needed; turning it *off* always takes effect immediately but additionally asks (a `write-scope-dialog`-styled confirm overlay, reusing `useWriteScopeGate`'s existing visual pattern) whether to also bulk-clear notify from current favorites — declining leaves every per-channel flag untouched. **Configuration preservation, explicit invariant:** neither switching `notifyScope` nor toggling `notifyNewVideos` off/on ever writes to the per-channel `notify` column — both are pure read-side filters (`main.ts`'s `maybeNotifyNewVideos`), so a "Selected Channels" configuration survives being ignored while "All Channels" or the global toggle is temporarily active, and reappears exactly as left once switched back. Per-channel control moved out of the sidebar's "⋯" overflow menu into a **directly clickable, always-visible icon** (`●` filled / `○` outline — plain glyph, no emoji, matching the existing `★`/`☆` favorite convention) placed immediately before the "⋯" button, shown only when `notifyNewVideos && notifyScope === 'selected'` (hidden the rest of the time since it would have no effect) — and the identical control was added to the channel details page next to the existing `★`/`☆` Favorite button, both driving the same `toggleChannelNotify` IPC call so either surface always reflects the other. New repository primitives backing the convenience toggle: `setChannelNotify` (direct set, not toggle) and `bulkSetNotifyForFavorites` (`UPDATE ... WHERE favorite = 1`) — no schema change, same `account_channels.notify` column from the original pass. **Revised again same day** (UI polish + a live-tested tray fix): the ●/○ dot glyphs were replaced with a small inline monochrome SVG bell (`src/ui/icons.tsx`, `currentColor`-based, filled/outline mirroring ★/☆) since Unicode has no non-emoji bell character (🔔/🔕 default to colored emoji presentation) — the owner asked for an actual bell shape once emoji were ruled out. Favorite was also moved out of the sidebar's "⋯" menu into its own always-visible ★/☆ row button (same pattern as the notify icon), leaving only Unsubscribe in the menu. **Tray duplication, root-caused via live testing, not guesswork:** the owner's live retest, after the single-instance-lock fix, still hit stuck/duplicate tray icons **within one running process** (toggling the Settings checkbox alone, no relaunch involved) — narrowing the fault to `destroy()` itself not being honored while the process stays alive. Live D-Bus introspection (`busctl --user call org.kde.StatusNotifierWatcher ... RegisteredStatusNotifierItems`) during a stuck-icon state showed **zero** live Chronicle registrations, while the ghost icons stayed visible and stopped responding to clicks/context-menu — proof they were dead delegates in the tray host's (QuickShell, `qs -p .../dms`) own rendering state, not anything Chronicle still held open. Quitting the app fully (which drops the whole D-Bus connection rather than sending an explicit unregister) reliably clears the icon, confirming the host handles connection-loss cleanup fine but not an explicit live unregister. Two speculative fixes were tried and both reverted: (i) `tray.setContextMenu(null)` before `destroy()` — unproven two-step teardown, no confirmed improvement; (ii) a fixed `guid` on `new Tray(icon, guid)` — Electron validates this against a strict format and threw `Invalid GUID format` live on this exact setup (caught by the existing try/catch, so no crash, but no tray got created either) — reverted outright once confirmed **not** the free/harmless addition it looked like from the type signature (`getGUID()` is documented `@platform darwin,win32` only). **Adopted fix (owner's own call, accepting the trade-off):** the tray is no longer destroyed when "Run in background" is turned off mid-session — only at real app quit (`will-quit`) or a `deleteAllData` reset (a genuinely new boot() generation, where stale closures are the actual concern). `backgroundModeEnabled` still fully governs whether closing the window quits or hides, so the setting still does what it says for that behavior; the one accepted regression is that an already-shown tray icon no longer disappears the instant the toggle goes off — it lingers until Chronicle actually quits. Settings' note text was updated to say so plainly. **Revised again same day — new setting, startMinimized (owner's request):** "Start minimized to tray" — only takes effect when autoStart and backgroundMode are both also on (an autostart launch with no tray to fall back to would leave a fully unreachable, windowless process); a manual launch always shows the window regardless. Detecting *why* a given launch happened turned out genuinely platform-split, found while checking Electron's own type declarations rather than assumed: **macOS** reports it natively (`app.getLoginItemSettings().wasOpenedAtLogin`, no custom argument needed) — its `openAsHidden`/`wasOpenedAsHidden` pair exists but is deprecated and explicitly documented as not working on macOS 13+, so it's read-only detection here, never used to suppress the window. **Windows** has no equivalent per-launch signal, so `applyAutoStart()` bakes a `--hidden` flag into the login item's own `args` (Electron's `Settings.path`/`args` are `@platform win32`-only — do not exist for `darwin` at all) and `wasLaunchedViaAutostart()` reads it back from `process.argv`. **Linux** has no Electron-level login-item API regardless (per the original pass's own note), so the same `--hidden` flag doubles as its detection too, appended to the hand-written `.desktop` entry's `Exec=` line. The flag is always included whenever autostart is enabled at all (not conditionally on the current `startMinimized` value), so toggling `startMinimized` alone — without retoggling `autoStart` — still takes effect on the next login; whether to actually skip the window is decided at boot from the live settings, not from what happened to be baked into the launch arguments. Guards against firing on a `deleteAllData` reset's re-`boot()` (window already open, `process.argv` unchanged) via a `hasBootedBefore` flag limiting it to the real launch. **Also fixed same day, found by checking rather than assuming:** `process.execPath` alone is only correct for a packaged app — running from source (`electron-vite dev`) it's the bare Electron binary with no app to load at all (fixed by appending the project entry point, `process.argv[1]`, as an argument in dev); and an AppImage's `process.execPath` resolves *inside its own temporary SquashFS mount*, torn down the moment that run exits — writing that into the autostart entry would silently break on the very next login despite working during the session that enabled it, so `$APPIMAGE` (the runtime's own stable path to the actual `.AppImage` file) is used instead whenever present. | `features.md`, `architecture.md`, `non-goals.md`, `local-data.md` |
| D-049 | **Ordinary per-cycle RSS sync failures no longer show a banner — only a systemic failure does (every polled channel failing at once, on a poll wide enough that random noise couldn't explain it)** (`bugs.md` [[B-110]]) — supersedes the original behavior, which showed a "{count} channel(s) failed" banner (with expandable per-channel details) any time `SyncReport.outcome === 'partial'`, i.e. any cycle with at least one failed and one succeeded channel. Once [[D-048]]'s live investigation confirmed YouTube's own RSS backend routinely returns transient 404/500 for genuinely active channels — independent of Chronicle, non-deterministic (the same channel can fail one cycle and succeed the next) — it became clear `outcome: 'partial'` would be true on essentially *every* cycle forever (never converging to zero, since it's a rolling noise floor across ~230 channels each queried independently), making the banner a permanent fixture the owner could never act on or dismiss for good — exactly the kind of "something on screen nobody put there for a reason" the calm/predictable-UI principle (`CLAUDE.md`) argues against, and worse, training the owner to stop reading sync banners at all (alarm fatigue), which would blunt a *real* failure signal too. **Rationale (the product owner's own call, 2026-07-16):** stop surfacing per-cycle partial failures entirely — they're retried automatically next cycle regardless (D-048) — and reserve the banner for a signal actually worth the owner's attention: every polled channel failing in the same cycle (`outcome === 'failed'`, gated on `channelsPolled > 1` so a single unlucky manual per-channel refresh, [[B-036]], can't trip it from ordinary noise alone) — a pattern random per-channel RSS flakiness essentially cannot produce on its own, so it's a reliable proxy for a real problem (e.g. the user's own network being down). **Exercised 2026-07-16:** `App.tsx`'s `refresh:done` handler now checks `outcome === 'failed' && channelsPolled > 1` instead of `outcome === 'partial'`; the old `app.banner.refreshPartial` string is replaced by `app.banner.refreshAllFailed`. Ordinary partial failures still get logged (`sync_log`, `SyncReport.failures`) for anyone who goes looking — they're just not pushed at the owner as a banner anymore. | `youtube-api.md` |

## Pending decisions

Ordered by how urgently they block work (see `roadmap.md`).

| ID | Decision | Options (recommended first) | Blocks | Spec |
|---|---|---|---|---|
| **D-008** | Storage engine | **SQLite (WAL)** · flat files · embedded KV | M0 (high confidence — treat as working plan) | `architecture.md`, `local-data.md` |
| **D-017** | Week-grouping start day | **fixed Monday (ISO)** · locale-dependent | M1 (trivial) | `feed.md` |
| **D-019** | Backfill depth for new/first-sync channels | **RSS window (~15) + scroll-triggered deeper backfill** · fixed 50 · configurable | M2 | `feed.md` |
| **D-020** | Auto-pruning of stateless old videos | **off by default** · on at 24 months | M5 | `local-data.md` |
| **D-021** | Watch Later queue auto-advance | **off by default, opt-in setting** · on (it's the user's own queue) · never | M3 | `playback.md` |

## Key assumptions to verify (tracked; promote or correct on verification)

| Where | Assumption | Verify in |
|---|---|---|
| `authentication.md` | Publishing an External consent screen to production without verification still works for the sensitive-not-restricted `youtube.readonly` scope, and removes the 7-day refresh-token expiry | M2 |
| `youtube-api.md` | Quota costs table (1-unit list calls, 100-unit search, 10k/day default) is current | M2 |
| `youtube-api.md` | Channel RSS feeds remain available, ~15 items, minutes-fresh | M2 |
| `youtube-api.md` | `UC…`→`UU…` uploads-playlist transform is reliable (would save a `channels.list` pass) | M2 |
| `youtube-api.md` | API data-retention policy window (~30 days) — confirm exact current rule | M2 |
| `feed.md` | RSS `published` reflects public availability for premieres/scheduled videos | M2 |
| `feed.md` | No Shorts flag in the API; duration + `/shorts/{id}` HEAD heuristic discriminates reliably — **now MVP-blocking (D-028), verify early in M2** | M2 |
| `local-data.md` | No runtime quota-remaining API exists (client-side accounting needed) | M2 |
| `architecture.md` | SQLite WAL comfortably handles 1 writer + 1 reader at ≤100k rows | M1 |
| `decisions.md` (D-050) | Windows/macOS auto-start (`setLoginItemSettings`) and OS notifications behave as expected on those platforms, incl. whether D-043's unsigned/non-notarized macOS build degrades Notification Center registration — owner's hands-on verification is Linux-only | owner's own Windows/macOS testing |

## How to add an entry

Next free ID: **D-051**. One decision per ID. State the options, the recommendation and
its rationale (or the final choice and why), the milestone it blocks, and the owning spec.
Never edit a Final entry's meaning — supersede it with a new entry and mark the old one
**Superseded by D-NNN**.

# Decision Log

The authoritative status of every design decision. Specs reference these IDs inline.

Statuses: **Final** (decided; changing it requires a superseding entry) ·
**Pending** (alternatives + recommendation documented; not confirmed by the product
owner) · **Superseded**.

Rationales live in the linked specs; this log is the index and the status of record.
When a Pending decision is confirmed, update its status here **and** remove the Pending
labels at its spec references, in the same change.

Entries with a long implementation narrative (multiple revision rounds, bugs found live)
keep only a condensed summary here — the full blow-by-blow moves to
`decisions-history/D-NNN.md`, one file per archived decision, linked from its row. This
mirrors how `tracker-current.md` rotates closed-out bug batches into `tracker-history/`:
the index stays lean, nothing is deleted. Only archive an entry once it's stable (Final,
not under active revision); a decision still being revised stays inline in full so it's
not fragmented across an active editing session.

## Final decisions

From the product brief (2026-07-10):

| ID | Decision | Spec |
|---|---|---|
| D-001 | Own-credentials model: every user creates their own Google Cloud project + Desktop OAuth client; Chronicle ships zero credentials | `authentication.md` |
| D-002 | Subscriptions-only chronological feed; no recommendations/trending/algorithmic feeds — the full "Never" list | `non-goals.md`, `feed.md` |
| D-003 | Video states (read/ignored/favorite/watch-later) are **Chronicle concepts, local-only, never synced to YouTube**. (Narrowed by D-032 on 2026-07-10: the original "readonly scope only" clause was superseded — see D-032. The local-state rule itself is untouched.) | `feed.md`, `authentication.md` |
| D-004 | Secrets in OS secure storage (libsecret/Keychain/Credential Manager); never uploaded anywhere | `authentication.md` |
| D-011 | Local-first & privacy-first: no Chronicle servers, no telemetry, no analytics, ever | `non-goals.md`, `local-data.md` |
| — | Desktop-first; minimal RSS-reader-style UI; dark mode first; keyboard shortcuts; backend owns OAuth/API, frontend owns UI; clean architecture with replaceable parts | `ui.md`, `architecture.md` |
| — | MVP scope = the features in `features.md` §MVP; comments optional and excluded from MVP | `features.md` |

Confirmed by the product owner (2026-07-10, decision session):

| ID | Decision | Spec |
|---|---|---|
| D-006 | Playback = **embedded YouTube IFrame player**, styled as clean as possible (minimal branding via `rel=0`/`modestbranding` params); per-video "open in browser" escape. Building our own player over extracted streams was **rejected**: TOS violation, breaks on YouTube internal changes, and deprives creators of ad revenue (views without monetization). mpv/yt-dlp path demoted to Future idea. **Revised 2026-07-17:** an earlier pass also added a Chronicle-drawn end-of-video overlay ("Back to feed"/"Next in queue") on top of the embed; removed — its only trigger was the raw `onStateChange` event, unreliable for embed-initiated transitions the same way B-111 already diagnosed for other player states, so it never actually fired in real use. The video just stops now; `rel=0` already keeps YouTube's own related-videos screen from showing. | `playback.md` |
| D-005 | Desktop shell = **Electron** (follows from D-006: Chromium guarantees embed playback reliability on all three OSes; Linux system webviews do not) | `architecture.md` |
| D-007 | Feed data source = **Hybrid**: sync subscriptions locally → RSS feeds of those channels for discovery (free) → batched `videos.list` hydration for metadata. Only recent videos fetched by default; older content loaded on demand. | `youtube-api.md` |
| D-012 | Testing-mode 7-day token expiry: wizard **explains and recommends** publishing the app to production but it is the **user's optional choice**. On every app open, Chronicle validates the connection; on `invalid_grant` it prompts re-auth via the **default browser** (low friction — the user is typically already signed in to Google and just confirms). | `authentication.md`, `onboarding.md` |
| D-010 | Video state model = **read-status enum (`unread`/`read`/`ignored`) + orthogonal `favorite` and `watch_later` flags**, because a video can be read AND favorite AND queued simultaneously (email-style: read state vs. star). | `feed.md`, `local-data.md` |
| D-027 | Older-content pagination = **continuous scroll** through the user's own local chronological archive (with on-demand deeper backfill per channel). This is bounded, mechanical scrolling through content the user chose — distinct from, and not a violation of, the ban on algorithmic infinite feeds. New content at the top remains finite ("caught up" state unchanged). | `feed.md`, `non-goals.md` |
| D-028 | ~~Shorts are never displayed. Not now, not ever. No toggle.~~ **Superseded by D-035** (2026-07-12, B-028). Detection heuristic (duration + `/shorts/` URL check) is unchanged and still specced in `feed.md`. | `feed.md`, `non-goals.md`, `features.md` |
| D-029 | **Universal video opening**: the player view opens **any** YouTube video, subscribed or not — YouTube links in video descriptions open in-app; a video can be opened by pasted URL. The recommendation ban targets *algorithmic selection*, not user-initiated navigation or creator curation. Externally opened videos never enter the feed and never count as unread; local states (favorite/watch-later) work on them. | `playback.md`, `feed.md`, `non-goals.md` |
| D-031 | **YouTube search, working like YouTube's search**: the user types a query and finds videos and channels across all of YouTube, including channels they don't follow; results open in Chronicle's player/channel view. It is a *deliberate tool* — inert until the user types; results are never injected into the feed or any other view. Costs: pasted @handle/URL → `channels.list`/`videos.list` (1 unit); free-text → `search.list` (100 units/query, ~100/day headroom). **Implemented 2026-07-12 (B-009):** a "Mine" / "YouTube" scope toggle next to the existing local `/` filter; free-text search only fires on Enter (never per keystroke), rendering a flat video+channel results list distinct from the feed. Pasted @handle/URL cheap path is a separate, pre-existing surface (`UrlPrompt.tsx`, D-029) — untouched by this change. **Revised same-day (`bugs.md` B-054):** the scope toggle is gone — the `/` field has exactly one behavior now, searching YouTube directly on Enter. It no longer re-filters the locally loaded feed at all (that live-filter-while-typing behavior was the actual source of the reported confusion, not just the toggle) — local caching stays invisible to the user, browsing already-followed channels is the sidebar's job. | `non-goals.md`, `features.md`, `youtube-api.md` |
| D-032 | **Scope policy: minimal at start, incremental by feature.** Initial auth requests `youtube.readonly` only; write actions (like, comment, subscribe) each request `youtube.force-ssl` incrementally on first use, with an explanatory confirm dialog before any consent popup (`useWriteScopeGate`), never silently. Chronicle-side local states (D-003) still never sync. **Known API gap:** YouTube Data API v3 has no endpoint to like a *comment*, only videos — shown read-only. **Correction found live:** reading comment threads actually requires the write scope too, not just readonly as first assumed. Full revision narrative: [decisions-history/D-032.md](decisions-history/D-032.md). | `authentication.md`, `non-goals.md`, `features.md` |
| D-030 | Channel view offers **both follow mechanisms**: (a) "Subscribe on YouTube" (`subscriptions.insert`, 50 units, incremental scope per D-032) and (b) "Follow locally" via RSS — Chronicle-only, invisible to the user's YouTube account. **Implemented 2026-07-12 (B-009):** mechanism (a) only, reachable from a YouTube search result's Subscribe button — subscribing runs the same incremental write-scope consent as [[B-010]]'s unsubscribe, then a channel-scoped sync to populate its videos. Mechanism (b), "Follow locally," is **not yet built** — that's D-033's accountless-mode work, still future. | `features.md`, `vision.md` |
| D-033 | **Accountless mode** (enabled by local follows): Chronicle is usable **without any Google/YouTube account** — follow channels locally via RSS, chronological feed, embedded playback, local states. The onboarding wizard becomes an optional path that unlocks account features (subscription import, search, like/comment, full metadata). Degraded-mode details (no `videos.list` hydration → no duration badges; Shorts detection via HEAD-check on every new video instead of duration-filtered candidates) specced in `features.md`. Lands together with D-030 (post-MVP #1); MVP still assumes the authenticated path. | `features.md`, `onboarding.md`, `authentication.md` |
| D-009 | Frontend framework = **React + TypeScript** (confirmed) | `architecture.md` |
| D-018 | Show view counts on feed rows = **on by default**, with a Settings toggle to hide. Supersedes the "hidden by default" choice exercised in M5 — the product owner reported the default as friction (B-029): the data costs no extra quota (captured at hydration) and is normal context on a video row. | `feed.md` |
| D-023 | Muted-thumbnail treatment = **rejected; thumbnails render at normal (full) opacity always**. The M3 prototype (dimmed at rest, full opacity on hover/selection) was reported as an unwanted default (B-034) and dropped rather than kept. | `ui.md` |
| D-035 | **Shorts are shown in the feed, tagged with a badge, with a "Show Shorts" Settings toggle (default on) to hide them.** Supersedes D-028's unconditional exclusion (2026-07-12, B-028) — the product owner argued unconditional exclusion contradicted the app's own "agency, not austerity" test: the feed only shows content from channels the user chose to follow, so filtering some of it without consent is the algorithm's will in miniature. Detection pipeline (duration candidate + `/shorts/{id}` HEAD confirmation) is untouched; only the display policy reversed. | `feed.md`, `non-goals.md`, `features.md` |
| D-037 | **List vs. grid feed layout, plus a shared file-explorer-style item-size control (xs/small/medium/large/xl) — both persisted, both inline in the feed topbar, not Settings.** Supersedes the original "one column only" Layout decision and D-022's two-step density — a thumbnail grid and a size preference are personal layout choices, not engagement mechanics, so the "who is driving?" test doesn't ban them. Landed inline (icon toggle + slider) after the owner moved it out of Settings mid-implementation. Full narrative (incl. a CSS-specificity bug affecting grid thumbnails): [decisions-history/D-037.md](decisions-history/D-037.md). | `ui.md` |
| D-034 | **M0 toolchain** (2026-07-11): **npm** as package manager (explicit product-owner choice — no pnpm), **electron-vite** (dev/build for main+preload+renderer), **node:sqlite** (Node's built-in synchronous SQLite driver), **Vitest** (unit tests), **ESLint + Prettier**, **dependency-cruiser** enforcing the architecture.md layer rules as lint errors. (Amended same day by product owner: standalone GitHub Actions CI removed from M0 — a single smarter pipeline with releases + automatic binary builds will be designed later, see `roadmap.md` M5. Amended in M1: better-sqlite3 replaced by node:sqlite — no native module, no Electron ABI rebuild, and storage contract tests run under the system Node in Vitest; stable in Electron's Node 24, experimental-but-functional in system Node 22.) All replaceable without touching `core/`/`ipc/`. | `architecture.md`, `roadmap.md` |
| D-038 | **Default playback speed**, a Settings → Playback dropdown over the YouTube IFrame API's fixed rate set (0.25×–2×, default 1× "Normal"). The player opens already set to this rate — issued once the embed announces itself and re-issued on the first `onStateChange(playing)` event, mirroring D-006/B-038's quality-reissue pattern, since YouTube can reset a requested rate the moment the stream actually starts. Per-video changes made from the embed's own speed control are session-only and never write back to the setting (product-owner request, 2026-07-12). | `playback.md` |
| D-040 | **Multi-account schema (B-003):** a channel followed by more than one local account splits into shared/deduped channel facts (`channels`) + an `account_channels` junction table for the per-account relationship (subscribed/favorite/subscription id/backfill cursor). **Exercised 2026-07-12:** a simpler single `account_id` column added directly to `channels` was considered and rejected mid-implementation — it would let one account's re-subscribe silently overwrite another's row for the same channel, a correctness bug for a feature specifically about supporting more than one account. The junction table is the only one of the two that's correct when two accounts follow the same channel. | `local-data.md` |
| D-041 | **Multi-account scope simplifications**, exercised alongside D-040, all deliberate and narrower than a maximal implementation would be: **(a)** Settings' Connection section and the first-run wizard keep managing one "primary" account only — completely unchanged UX for existing single-account users; all additional accounts are managed through a new sidebar Accounts section instead. **(b)** The primary account cannot be removed via the Accounts section (only Settings' existing Sign Out) — the Remove option is disabled with an explanatory tooltip for the primary account (fixed 2026-07-12, `bugs.md` [[B-066]]; previously failed silently). **(c)** Subscribing to a new channel found via search ([[B-009]]) always subscribes under the primary account — no account picker; an action on a channel followed by more than one account applies to whichever account owns it first if more than one does (rare: two of *your own* accounts both following the identical channel). | `local-data.md`, `authentication.md`, `ui.md` |
| D-039 | **Favorited-channel priority section (B-042):** duplicate placement — a favorited channel's unread video appears in **both** the priority section and its normal chronological bucket, never removed from the bucket. **Exercised 2026-07-12:** consistent with D-010's orthogonal-flags model, where favorite/watch-later never remove a video from its normal spot — a video being prioritized shouldn't make "did I see everything from Tuesday" miss it from Tuesday's bucket. The priority section only lists unread videos (parallel to the unread view), capped at 20. Still rough per `bugs.md` [[B-063]]: the section doesn't yet follow the feed's layout/item-size settings. | `feed.md` |
| D-042 | **Archive-backfilled videos default to read, not unread (`bugs.md` B-058):** a channel's deep back-catalog (`SyncService.backfillArchive`, D-027/B-002) predates the user actively following/using Chronicle — it was never "missed" chronologically, so surfacing it shouldn't inflate unread counts app-wide. **Exercised 2026-07-12:** `SyncRepository.markVideosReadIfUnset` inserts a `video_state` row with `read_status = 'read'` for every newly archive-backfilled video, `ON CONFLICT DO NOTHING` so it never overwrites a real state row from another path (e.g. hydrate-on-open via search). Deliberately **not** applied to routine sync's gap-backfill (`backfillGap`) — those videos are genuinely missed uploads since the last sync, not archive history, so they stay unread as before. | `feed.md` |
| D-024 | **Linux packaging = AppImage only for the first release.** Supersedes the original "Flatpak primary + AppImage" recommendation — the product owner deferred Flatpak's extra manifest-maintenance/store-submission overhead until there's demand for it. AppImage is a single portable binary with zero extra publishing infrastructure, built straight from `electron-builder`. Flatpak stays a Future idea; revisit post-first-release. **Exercised 2026-07-13.** | `roadmap.md` |
| D-025 | **Windows builds ship unsigned.** The NSIS installer is not code-signed; Windows SmartScreen shows its "Windows protected your PC" warning on first run, with a documented bypass (More info → Run anyway) in the README. **Exercised 2026-07-13.** | `roadmap.md` |
| D-043 | **macOS builds ship unsigned, not notarized.** The dmg is not signed or notarized; Gatekeeper blocks the unidentified app by default, with a documented bypass (right-click → Open, or System Settings → Privacy & Security → Open Anyway) in the README. This wasn't previously a tracked Pending decision — `roadmap.md`'s M6 bullet assumed a "notarized dmg" as fact; this entry corrects that assumption to match what's actually shipping. **Exercised 2026-07-13.** | `roadmap.md` |
| D-026 | **Update mechanism = automatic background check against GitHub's public Releases API** (`GET /repos/AdrianoMoura/ChroniclePlayer/releases/latest`), not a hand-rolled signed static manifest as originally recommended. The privacy invariant this decision protects — no identifiers or telemetry sent — is satisfied by an unauthenticated GET that only asks "what's the latest tag"; a custom manifest would need its own signing/publishing infrastructure for no additional privacy benefit, and the API's `tag_name` already *is* the version, so there's nothing extra to keep in sync. Checks once at startup and every 24h from a main-process timer, compares semver to `app.getVersion()`; on a newer version, shows a dismissible in-app banner linking to the release page — **never** auto-downloads or auto-installs (would fail the "who's driving?" test). Off-switch in Settings, default on. **Exercised 2026-07-13.** | `roadmap.md` |
| D-013 | **Secret-store fallback when no OS keychain = Electron `safeStorage`, else machine-derived key + honest warning** (rejected: refuse to persist). **Exercised in M2 (2026-07-11):** safeStorage when a real keychain backs it; else AES-256-GCM under a scrypt key from `/etc/machine-id`+user, `isSecure()=false` → UI warning. The writing cipher is pinned in the store file. Real-world smoke (product owner, keyring-less niri session) hit the missing-fallback crash on day one — `safeStorage.encryptString` throws without a keychain — confirming the fallback is mandatory, not theoretical. | `authentication.md` |
| D-016 | **Background refresh default interval = 30 min, configurable 15 min–manual** (rejected: fixed 15 min or hourly). **Exercised:** 30-min default (M2); configurable 15/30/60/manual in Settings (M5, 2026-07-11). | `youtube-api.md` |
| D-014 | **Wizard validation for Google-console steps 1–5 = checkbox confirmations + failure→step mapping at connect time** (rejected: attempt live validation). **Exercised in M4 (2026-07-11).** | `onboarding.md` |
| D-015 | **Wizard media = static annotated screenshots** (rejected: per-step recordings). **Exercised in M4 (2026-07-11):** asset pipeline with verified-on dating built. Capture of the actual screenshots is still open, tracked as an M6 exit item in `roadmap.md` — the decision (format) is settled, only the asset-capture work remains. | `onboarding.md` |
| D-044 | **Wizard Step 8 ("First sync") removed as a blocking step** (`bugs.md` [[B-090]]) — supersedes the original design where the wizard waited for the first sync to finish before unlocking "Open feed." Step 7's successful connection already triggers a first sync in the backend (the same `connectGoogle` path used ever since M2); the wizard step was a second, redundant trigger for that same sync, gating on an event stream the not-yet-mounted step could easily miss (root cause of the button getting stuck disabled). **Exercised 2026-07-15**, per the product owner's own stated preference: the wizard now finishes the instant Step 7 connects, landing on the feed screen with sync continuing in the background exactly like every later refresh already works. | `onboarding.md` |
| D-045 | **Embedded-player session = Electron's ordinary default session (no custom partition), shared with a new "Sign in to YouTube" action** — chosen over per-cookie injection between separate sessions, since Chronicle's own OAuth flow always ran in the system browser and never touched Electron's browsing session, leaving the embed unauthenticated. Settings → Connection gained a "Sign in to YouTube" button opening a same-session window (no automation/scraping). **Revised same day:** a first attempt used a *named* persistent partition instead, which broke every thumbnail (`thumb://` only registers on `session.defaultSession`) — reverted to the default session. Full narrative: [decisions-history/D-045.md](decisions-history/D-045.md). | `authentication.md` |
| D-046 | **Miniplayer continuity: docking to the corner keeps the same live `<iframe>` mounted (no restart); extracting to an always-on-top window cannot, and hands off a playback snapshot to a fresh instance there instead.** Two different technical problems disguised as one question — extraction opens a genuinely separate renderer process (the DOM node can't move there), while docking stays in the same process, so the video *should* survive it. **First attempt (a React portal) was wrong:** moving an `<iframe>` between DOM parents makes Chromium reload it, a browser behavior, not fixable by portaling more carefully. Fixed by keeping `PlayerSurface` at one stable tree position always, mirroring whichever slot (full-view or corner) is active via measured `position: fixed` styles. **Eight same-day revision rounds followed**, each surfaced only by the owner's own live testing: navigation-away paths bypassing the dock decision entirely; an overly strict `PLAYING`-state gate loosened into `isStillGoing()`; a missing extraction return-path and unreliable autoplay; then six further gaps (resize-handle stacking, extract-autoplay wiring, default-speed reissue, full-page scroll) each root-caused and fixed in turn. Full narrative: [decisions-history/D-046.md](decisions-history/D-046.md). | `playback.md` |
| D-047 | **On an account's very first sync, every discovered video is marked read on arrival — not just the backlog published before today** (`bugs.md` [[B-105]]) — supersedes the original B-020/B-069 rule, which only auto-read backlog videos and left same-day videos unread. **Superseding rationale (the product owner's own call, 2026-07-16):** a first sync has no prior visit to judge "new since you were last here" against — every video it discovers, today's included, is an equally uninformed guess about relevance, so assuming any of it is unread-worthy is an unjustified assumption, not a real signal. Every subsequent sync (routine or backfill) is unaffected and keeps marking newly discovered videos unread as normal — this only changes the account's very first run. **Exercised 2026-07-16:** `SyncService.refresh`'s `firstSync` branch now calls `repo.markVideosReadIfUnset` on every video in each hydrated batch unconditionally, instead of filtering by `publishedAt` against a same-day cutoff (the `backlogCutoff`/`startOfToday` logic was removed outright, not just widened). | `feed.md` |
| D-048 | **Removed the "channel unavailable" permanent state entirely — an RSS 404 is just an ordinary retried-next-cycle failure now.** The prior behavior (never a real signed-off decision, just an unreviewed assumption baked in during earlier development) permanently excluded a channel from every future sync after a single transient RSS 404, with the promised "settings list" surfacing it never built — a real, silent bug (B-110). **Rationale:** RSS calls cost no quota, so there's no reason to ever stop retrying; a single failure is never proof of anything permanent. Full narrative: [decisions-history/D-048.md](decisions-history/D-048.md). | `youtube-api.md` |
| D-050 | **Tray-resident mode, OS auto-start, and opt-in new-video notifications — three fully independent Settings toggles, default off, none gates another.** Auto-start uses Electron's built-in login-item API on Windows/macOS and a hand-written XDG `.desktop` entry on Linux (no native module); "Run in background" hides to tray instead of quitting (Electron's `Tray`), keeping the D-016 sync timer alive; notifications use Electron's `Notification` API, scoped **All Channels** / **Selected Channels** (a per-channel `notify` flag, schema v9) with an auto-enable-on-favorite convenience. Never fires on an account's first sync (mirrors D-047). **Real bugs found only via live testing:** a missing `app.requestSingleInstanceLock()` caused duplicate tray icons across relaunches; a tray-host staleness bug (confirmed via live D-Bus introspection) worked around by never destroying the tray mid-session, only at real quit; and three separate auto-start bugs (dev-mode's bare-binary launch, an AppImage's temporary-mount `process.execPath`, and the platform-split "was this launch autostart" detection needed for the later "Start minimized" setting). Full narrative (multiple same-day revision rounds): [decisions-history/D-050.md](decisions-history/D-050.md). | `features.md`, `architecture.md`, `non-goals.md`, `local-data.md` |
| D-051 | **Closing the window to the tray with a video still playing no longer leaves it running silently — it pops out to the always-on-top extract window (same as `p`), or pauses, per a new `popOutOnClose` Settings toggle (default true).** A direct product-owner request prompted by a real bug hit live: tray-close (D-050) just hides the window, so a playing video kept running with no way to stop it short of reopening from the tray. `extractPlayer` gained an `auto` flag so an auto-popped window's close doesn't restore playback into the still-hidden main window, plus a `title` param so the extract window's OS-level title differs from the main window's (relevant on the owner's niri/Wayland compositor). **Regression found and fixed same session:** the on-screen Extract button briefly stopped popping out (just closed the player instead) once `extractToWindow` gained a parameter — React forwards the raw `MouseEvent` to any `onClick` handler regardless of its declared type, and that isn't structured-cloneable over IPC; fixed by splitting into a parameterized internal function plus a permanent zero-arg wrapper safe to bind to any handler. Full narrative: [decisions-history/D-051.md](decisions-history/D-051.md). | `playback.md` |
| D-052 | **Notifications now respect Shorts the same way the feed does, plus an independent `notifyShorts` toggle.** Bug: `newVideosByChannel`'s tally was built from RSS-discovery counts *before* `confirmShorts()` ran, so a Short always counted as a normal new-video notification regardless of `showShorts`. Fixed via a per-channel `shortsCount` resolved right after that cycle's Shorts verdicts settle. **Combined rule:** `includeShorts = settings.showShorts && settings.notifyShorts` — a Short hidden from the feed never notifies regardless of the new toggle. Full narrative: [decisions-history/D-052.md](decisions-history/D-052.md). | `features.md` |
| D-053 | **A currently-live video sorts to the top of its date bucket; an ended broadcast sorts and buckets by when it actually ended, not its original (older) `publishedAt`.** One design was rejected mid-conversation (a separate bucket-less "Live now" section, mirroring D-039) — the owner wanted this to stay "just ordering," no new section. A single `effectiveDate(video, now)` value now drives both bucket assignment and sort order (never two separate timestamps), after an earlier pass caused a broadcast crossing midnight to sort right but bucket wrong. Display-only in `FeedService.getSlice()` — never touches the keyset pagination cursor (D-027), which stays on raw `publishedAt`. Also captures `liveStreamingDetails.actualEndTime` (new `videos.live_ended_at`, schema v12, sticky) and extends the feed's "ended" badge to any video whose end is known even without ever being caught live. Full narrative: [decisions-history/D-053.md](decisions-history/D-053.md). | `feed.md`, `local-data.md` |
| D-008 | **Storage engine = SQLite (WAL)**, single file in the app data directory. Rejected: flat files, embedded KV. Was the working plan from day one (M0, 2026-07-10) and never revisited; specific driver later pinned by D-034 (**node:sqlite**, no native module). **Exercised.** | `architecture.md`, `local-data.md` |
| D-017 | **Week-grouping start day = fixed Monday (ISO)**, not locale-dependent. Rejected: locale-dependent. **Exercised at M1 (2026-07-11)** with its recommended option. | `feed.md` |
| D-019 | **Backfill depth for new/first-sync channels = RSS window (~15) + scroll-triggered deeper backfill.** Rejected: fixed 50, configurable. **Exercised 2026-07-12 (B-002):** scrolling to the end of a channel-filtered view with no local pages left triggers `SyncService.backfillArchive`, paging the channel's uploads playlist on demand. | `feed.md` |
| D-020 | **Auto-pruning of stateless old videos = off by default.** Rejected: on at 24 months. **Exercised at M5:** the recommended "off" option needs no code to satisfy — nothing prunes today; an on-at-24-months setting can still ship post-MVP if ever requested. | `local-data.md` |
| D-021 | **Watch Later queue auto-advance = off by default, deliberate action only (`n` key).** Rejected: on by default (it's the user's own queue), never. **Exercised at M3.** | `playback.md` |
| D-049 | **Only a systemic sync failure shows a banner now — ordinary per-cycle RSS noise doesn't.** Supersedes the old `outcome: 'partial'` banner, which fired on nearly every cycle once D-048 confirmed YouTube's RSS backend routinely 404s transiently for active channels — a permanent, un-actionable fixture that risked alarm fatigue masking a real failure. Banner now gated on `outcome === 'failed' && channelsPolled > 1` (every polled channel failing at once). Full narrative: [decisions-history/D-049.md](decisions-history/D-049.md). | `youtube-api.md` |
| D-054 | **Localization: a Settings language dropdown (first section on the screen), defaulting to "Follow system," backed by a locale registry discovered at build time (`import.meta.glob` over `src/ui/i18n/locales/*.ts`) — not a hand-maintained list**, so contributing a translation is only a PR adding one file. English is the complete source-of-truth dict; any other locale may be partial, falling back to English per missing key. Ships with English + Portuguese (Brazil). **Revised same day, the owner's own live catch:** three call sites (`Sidebar.tsx` view labels, `App.tsx` bucket headers, the wizard's console-step text) had baked `t()` into a module-level constant evaluated once at import time, staying stuck in whichever language loaded first — fixed by converting them into functions called fresh at render time. **Also added:** a language dropdown on the onboarding wizard's own Welcome screen, since the wizard runs before Settings is reachable. Full narrative: [decisions-history/D-054.md](decisions-history/D-054.md). | `ui.md`, `architecture.md` |
| D-055 | **Player "up next" card: on video end, a floating bottom-right card suggests the next video from the user's own Watch Later queue** — thumbnail + title + explicit Open button, no autoplay, dismissible. Distinct from D-021's `n`-key shortcut (only active when the video was opened *from* the Watch Later view) — this is derived purely from data, so it also covers any other path. **FIFO rule:** the video that ended isn't queued → suggest the oldest-queued item; it is queued → suggest whichever follows it. Full-view only, never the miniplayer or extract window. **Fixed the same day, the owner's own live catch:** the card never appeared at all — reaching "ended" is always embed-initiated, and the one-shot `onStateChange` event doesn't reliably report embed-initiated transitions (the same bug class B-111 already found); fixed the same way, via the `infoDelivery` heartbeat as a second detection path, guarded to fire exactly once per real transition. Full narrative: [decisions-history/D-055.md](decisions-history/D-055.md). | `playback.md`, `non-goals.md` |
| D-056 | **Live chat panel on the player screen, via YouTube's own public `live_chat` embed iframe** (zero quota cost, no new scope) — a toggle next to the title (shown only while `liveContent === 'live'`, covering Premieres too) opens a 500px docked column, always starting closed. Closes automatically only when the video docks to the miniplayer; a separate manual "extract chat" action pops it to its own titled window, fully decoupled from the video's own extract (D-051). Typing requires the same signed-in embedded-player session (B-093) — a hint links to the existing sign-in IPC. **Merged to main 2026-07-23, live-tested and confirmed working.** **Fixed during the owner's live test:** the docked column rendered blank — the iframe was missing `embed_domain`, required when framed (`embed_domain=localhost`, since Chronicle's renderer always runs there). Also relabeled the toggle, repositioned the column's own extract button, and added a tooltip explaining the separate sign-in. Full narrative: [decisions-history/D-056.md](decisions-history/D-056.md). | `playback.md`, `youtube-api.md` |
| D-057 | **Three Watch Later refinements, all direct product-owner requests, same session:** (1) `SettingsDto.watchLaterAutoRemove` (default off) — opening a queued video removes it from Watch Later immediately, same effect as a manual untoggle. (2) The up-next card (D-055) now wraps around past the last queued video instead of going silent. (3) Drag-and-drop reorder in the Watch Later view, list and grid alike — the whole row/card is the drag source; drop-target hit-testing lives on `FeedList`'s own per-item wrapper (the exact virtualized slot, no dead zone), with a container-level fallback for empty space past the last item. Each video's own drop zone means "insert after it"; only the first video also accepts "insert before." Confirmed working live. Full narrative: [decisions-history/D-057.md](decisions-history/D-057.md). | `feed.md`, `playback.md` |
| D-058 | **User-created local Playlists — a direct product-owner request, not sourced from `tracker-current.md`, same pattern as D-050/D-053/D-055/D-057.** A new sidebar entry at position 4 (between Watch Later and Favorites; `Sidebar.tsx`'s `NAV_ORDER` interleaves it with the five `FeedView`s, so keyboard `1`-`6` still map 1:1 to the rendered list) opens a Playlists screen: every local playlist as a card/row (name, video count, `h:mm` total duration, and a composite cover built from its own first 1-6 video thumbnails, `PlaylistThumb.tsx`, arranged in a grid inside the exact same `.thumb` footprint a single video occupies at every itemSize). Playlists are 100% local (schema v17: `playlists` + `playlist_videos`, `ON DELETE CASCADE`) — never a YouTube playlist, never synced, same "Chronicle-only state" rule as D-003's read/favorite/watch-later flags. A playlist's own detail screen mirrors `ChannelHeader`'s compact style, with inline name/description editing (a pencil icon on hover, per the owner's own spec) and a delete button (double-click-to-confirm, mirroring Unsubscribe); its video list reuses `FeedList` directly (not the paginated main-feed pipeline) with drag-and-drop reorder, same mechanism D-057 built for Watch Later. Opening a video from a playlist never removes it (unlike Watch Later's opt-in auto-remove, D-057) — the explicit "remove from playlist" action (a new `VideoActions.removeFromPlaylist`, shown only in a playlist's own screen) is the only way membership shrinks. Every video card/row everywhere (feed, channel, watch later, player) gained a new "Add to Playlist" action (`VideoActions.addToPlaylist`, always provided) opening `AddToPlaylistDialog` — a checklist of every local playlist plus an inline create-and-add field, so the user never has to leave what they're doing to set up a destination first. On video end, the existing D-055 "up next" card now also covers a playlist context: opening a video from a playlist's own screen sets a lightweight nav-context ref (`playlistId`/`playlistName`/`videoIds`); if the video that just ended is a member, the card reads "Next in {playlist name}" and suggests `getNextInPlaylist`'s answer instead of the Watch Later queue's. The `videoIds` membership check is a deliberate self-correcting guard against a stale context (the ref is intentionally *not* cleared at every navigation call site, mirroring how the existing Watch Later `queueRef`/`hasQueueNext` pair already tolerates staleness the same way) rather than an exhaustive clear-everywhere pass. **Revised the same day, the owner's own live catch:** `nextInPlaylist` (`core/playlist.ts`) started as a direct reuse of D-055/D-057's `nextWatchLaterAfter`, including its wraparound — but the owner pointed out a playlist shouldn't wrap the way Watch Later deliberately does (D-057): Watch Later is a rotation to dip in and out of, a playlist is a curated collection with a real end. Reimplemented as its own (non-wrapping) function: last video ends → no suggestion, rather than looping back to the first. **Also caught live, a real architecture bug, not a one-off polish item:** the Playlists screen was originally its own top-level ternary branch sibling to the main feed's, each with its *own* copy of the player-rendering JSX — this meant `<PlayerSurface>` (a live YouTube iframe) literally unmounted and remounted (the video visibly reloading) every time `screen` flipped between `'feed'` and `'playlists'` while a video was docked, and the miniplayer's own `e`/`x` shortcuts (B-105) were unreachable from the Playlists screen since that branch's keydown handling always returned before reaching the main switch that owns them. Fixed by unifying the two branches into one shared layout — a single, always-mounted `.feed-region` whose *inner* content (Playlists screen vs. the normal feed) is what actually varies by `screen`, with the player block rendered exactly once as a stable sibling within it — so `<PlayerSurface>` keeps the same position in the render tree (and the same live iframe) across the transition, and the shared keydown branch now also owns `e`/`x` explicitly rather than only the unreachable main switch. **A separate, unrelated live catch found while testing the same build:** every dialog's own Escape handling turned out to only work if focus happened to already be inside it (an autoFocus text input, e.g. the URL prompt) — a dialog opened via a plain button or a keyboard shortcut with no such input (Add to Playlist, Help, write-scope consent, Add Account) left focus on whatever triggered it, a *sibling* of the dialog, so Escape bubbled straight past it to the player's own close/dock map underneath instead of ever reaching the dialog's handler. Fixed generally, not just for Add to Playlist: each affected dialog now focuses its own container on mount (`tabIndex={-1}` + a mount effect), so the keydown handler it already had is actually reachable regardless of how the dialog was opened. | `feed.md`, `local-data.md`, `ui.md` |
| D-064 | **Comments: an Edit action on the viewer's own comments and replies.** A direct product-owner request ("queria a opção de poder editar meus comentários"), not sourced from `tracker-current.md`, same pattern as D-050–D-063. **Confirmed working live by the product owner.** Maps onto `comments.update` (50 units, write scope, previously unused) — works on both a top-level comment id and a reply id, since both are `comments` resources. YouTube itself 403s if the authenticated user isn't the comment's author, so Chronicle only needs to decide when to *show* the affordance, not re-derive the authorization rule: the `snippet` part `commentThreads.list` already fetches also carries `authorChannelId.value` for free (previously discarded, same "surface what's already there" pattern as D-062's `viewerRating`), compared against the connected account's own channel id — also free on the existing `channels.list mine=true` call (`getOwnChannel`, previously only returning `title`; the `id` field rides the same response). Fetched once per comments-panel open via the existing `getConnectedChannel` IPC (until now only called by the onboarding wizard), not persisted — matches the panel's own "resolved once per open" lifetime as its loaded comment list. Clicking Edit on an own comment/reply swaps its text for an inline textarea (Save/Cancel), reusing the exact composer styling `comment-composer.reply` already established. No schema change, no new column — `authorChannelId`/`channelId` are request-scoped fields threaded through `Comment`/`CommentDto`, never written to SQLite. **Same-day live catch:** the new Edit button sat flush against the existing Reply button with no gap — both used the same bare `.comment-reply-toggle` class, which only ever had one instance per comment before this. Fixed by wrapping both in a `.comment-actions` flex row with a small gap. | `youtube-api.md` |
| D-062 | **Comments: an "open in browser" button per comment/reply, plus a red-heart indicator when the viewer already liked that comment.** A direct product-owner request. **Confirmed working live by the product owner.** YouTube's public API still has no endpoint to *like* a comment (D-032, unchanged) — but the same `snippet` part `commentThreads.list` already fetches also carries the viewer's own existing `viewerRating` ('like'/'none') for free, previously discarded; now surfaced as a red ♥ (`#e0a3a3`, the app's existing danger/highlight tone) next to the like count. The open-in-browser button (↗) mirrors the exact existing pattern from `ChannelHeader`/`FeedList` (a small bordered icon button with a `title` tooltip), linking to YouTube's own comment-permalink format (`watch?v=<video>&lc=<commentId>`). **Same-conversation follow-up, the owner's own catch:** opening the link left Chronicle's own copy still playing behind the new browser tab — fixed by threading the existing `onPause` callback (already used by the player's own B-121 open-in-browser action) down through `CommentsSection` → `CommentItem`/`ReplyItem` → `CommentAuthorRow`, called before the external URL opens, same fix shape as B-121. No schema change, no new IPC channel (reuses `openExternalUrl`), no quota cost. | `youtube-api.md`, `non-goals.md` |
| D-063 | **Comments: a sort-order toggle, "Top comments" (default) vs. "Newest first."** A direct product-owner request. **Confirmed working live by the product owner.** Maps straight onto `commentThreads.list`'s own `order` param (`relevance`/`time`) — previously hardcoded to `relevance` — via a small two-button segmented control in the comments panel (same "inline in the view, not Settings" precedent as D-037/D-060), always visible once the panel is open. Switching order resets the loaded page and re-fetches from the top rather than re-sorting client-side, since YouTube's own pagination cursor is order-specific. No schema change; the IPC `getComments` call gained an optional third `order` argument (default `relevance`, preserving prior behavior for any caller that doesn't pass one). Same 1 unit/page either way — a plain sort, not a new endpoint, so no quota impact. | `youtube-api.md` |
| D-059 | **Import a YouTube playlist into a local Playlist, plus a Sync action on an imported playlist.** A product-owner idea. **Confirmed working live by the product owner.** The Playlists screen (D-058) gains a second toolbar action, "Import from YouTube," next to "Create Playlist": paste any playlist URL (`youtube.com/playlist?list=…`), submit, and Chronicle creates a new local playlist — pre-named from the source playlist's own title/description — populated with the same videos in the same order. **Deliberately a one-time snapshot, not an ongoing background sync** — consistent with D-058 (playlists are 100% local, never synced to/from YouTube): nothing polls the source automatically. Instead, an imported playlist's own detail screen gains a **Sync** action: it checks (fetched fresh whenever that screen is opened, same "live, not persisted" precedent as the channel screen's own banner/subscriber-count fetch) how many videos the source has that the local copy doesn't, showing "Sync (N new)"; clicking it pulls just those in. **Sync is deliberately add-only** — it only appends videos the source has that the local playlist doesn't, in the source's own order, after the local playlist's current last item; it never removes a video the source removed, never reorders existing entries, and never touches name/description the user may have edited locally, consistent with D-058's "removal is its own explicit action" rule and with not clobbering the user's own curation. A schema addition, `playlists.source_playlist_id` (nullable; the source YouTube playlist id, null for an ordinary "Create Playlist" playlist), is what gates the Sync button's visibility. Mechanically, both the import and the sync reuse existing machinery end to end rather than adding new sync surface: `playlists.list` (1 unit, new call) for the source's own title/description, only at import time; the existing generic `listUploads` (`playlistItems.list`, 1 unit per 50-item page — already used for a channel's own uploads-playlist backfill, B-002, and works unmodified against *any* public playlist id) to collect video ids in order, reused as-is for the check and the sync too; then the existing D-029 `upsertExternalVideo` path (`videos.list` hydration, 1 unit per 50 ids) to locally register any video not already known — the common case, since a course/curated playlist's videos usually belong to channels the user doesn't follow — before appending each to the playlist via the existing `addVideoToPlaylist`. The "check" and "sync" IPC calls each redo their own independent page-through rather than sharing a cached diff — simpler code, and a second full walk of even a 200-video playlist is a few units, trivial against the 10,000/day budget (this project's own precedent: prioritize a frictionless, simple implementation over conserving quota headroom that isn't actually scarce, D-007's amendment). Total cost for importing a 200-video playlist: ~9 units. A per-video hydration failure (a deleted/private video inside an otherwise-importable playlist) is skipped individually rather than aborting the whole import/sync, with the import's own summary reporting "N of M imported"; a private or fully deleted *playlist itself* fails the whole import with a plain error in the dialog. **Revised the same day, the owner's own live catch:** the first pass gave no feedback at all during an import — a single IPC call that could run for several seconds with nothing on screen, which the owner tried once and gave up on assuming it had hung. Fixed by broadcasting a new `playlist:importProgress` backend→UI event (the same `chronicle:event` bus `refresh:progress` already uses) at each real step — fetching the source's title/description, once per `playlistItems.list` page while collecting video ids, and once per 50-video hydration batch — rendered as an append-only, auto-scrolling log between the URL input and the dialog's own buttons, with the dialog no longer dismissible (Escape/backdrop-click) until the request actually settles. Also fixed live: the toolbar's "Import from YouTube" button didn't vertically align with "+ New Playlist" (the reset `<button>` base style has no padding/border at all, unlike `.primary`, so it rendered at a different height) and read as too prominent for a secondary, occasional action — moved to sit before "+ New Playlist" (still visually secondary, `.subtle-btn`: same box model as `.primary` for alignment, smaller font, muted color). | `features.md`, `youtube-api.md`, `local-data.md` |
| D-060 | **Sortable channel sidebar list — a direct product-owner request, not sourced from `tracker-current.md`, same pattern as D-050/D-053/D-055/D-057/D-058.** Confirmed working live by the product owner (EN and PT-BR). `listFollowedChannels` (`repositories.ts`) still hardcodes one backend order — `favorite DESC, latest_published_at DESC (nulls last), title COLLATE NOCASE ASC` — but the sidebar now has a small control next to the "Channels" header (same "inline in the view, not Settings" precedent as D-037's list/grid + item-size toolbar) offering four modes, resorted client-side in `Sidebar.tsx` off the same `ChannelDto[]` already fetched (a new `latestPublishedAt` field added to `ChannelDto`/`getChannels` is the only backend-facing change, so "Recent" can sort without a second IPC round trip): **Favorites** (default — the backend's own order passed through unchanged: favorite channels first, each group then by its own most recent video, ties by name), **Recent** (all channels by most recent video only, favorite ignored), **Unread** (most unread videos first, off the `unreadCount` already carried on `ChannelDto` — zero extra cost, and fits the "who is driving?" test since it's the user's own reading backlog, not an engagement signal), **Name** (alphabetical, favorite ignored). **Deliberately session-only React state, not persisted to `settings.json`** — unlike D-037's layout/item-size, which do persist: the product owner's own framing was "ela sempre começa com por favoritos" (it always *starts* on Favorites), i.e. every fresh app launch resets to Favorites regardless of what was picked in a prior session, matching the "same action always produces the same result" predictability principle over "remember my last choice." The user can still switch modes freely within a running session. **First pass used a native `<select>`; the owner's own live catch:** its option popup is drawn by the OS (GTK on Linux) and ignores the page's CSS entirely — it rendered with a fixed white background and light text regardless of Chronicle's own dark/light theme, unreadable in light mode. Fixed by replacing it with a custom button + popup reusing the same `ContextMenu` portal component the channel/account "…" menus already use (plain themed divs, no native browser chrome involved) — `sortChannels` itself (unit-tested, `Sidebar.test.ts`) was untouched by this fix. **Also reported the same session: "Recent" appeared to just sort by name.** `sortChannels`'s `recent` branch was verified correct by a new unit test against realistic mixed dates/nulls (`Sidebar.test.ts`) — the comparator degenerating to a name-only order is exactly what happens when every `latestPublishedAt` compares equal, e.g. if the running app's Electron main process (which is what actually serves `getChannels` over IPC) predates this change and was never restarted, since only the renderer hot-reloads automatically in dev — a full app relaunch is needed to pick up new IPC-shaped fields like this. Flagged for the owner to re-verify after a full restart; no code change made against this half pending that confirmation — it turned out to be exactly that (a stale main process), and Recent sorts correctly after a full relaunch. **The custom popup itself then needed two more live-caught rounds before the whole control was confirmed working:** (1) the button's rendered width differs slightly between "Favorites ▾" and "Favoritos ▾" (EN vs. PT-BR), enough in some window widths to cross the shared `ContextMenu`'s threshold-based flip branch (`anchorRect.right - menuWidth < 8`, otherwise untouched since the channel/account "…" menus were built) and misalign the popup only in one language — replaced with a single left-based formula clamped to the viewport instead of a branching one, removing that boundary condition outright rather than chasing the exact trigger. (2) That fix then made the popup span the full window width: `.channel-menu`'s own CSS already sets `right: 4px`, and the new inline style set `left` without ever overriding `right` — with both non-auto and no explicit `width`, the box stretches to fill the gap between them instead of shrinking to content. Fixed by adding `right: 'auto'` to both inline style writes. Confirmed working live by the owner after this round (EN and PT-BR both). One further sort idea raised, not part of this ask: **manual drag-and-drop order** (mirroring D-057/D-058's existing reorder mechanism, for full free-form control) — larger scope, would be its own decision if wanted. | `ui.md`, `feed.md` |

## Pending decisions

| ID | Decision | Spec |
|---|---|---|

| D-061 | **Backup export/import (restore) — a direct product-owner request, not sourced from `tracker-current.md`, same pattern as D-050/D-053/D-055/D-057/D-058/D-060.** Design confirmed live in conversation and a full implementation plan approved; **not yet implemented** — no code written. The owner (a heavy daily user with many saved playlists) wants the existing one-way "Export data" (Settings → Data, `FORMAT.md`) to become a real backup/restore: leave with everything, reinstall elsewhere, come back configured. Two real gaps found while scoping this: the current export never included `playlists`/`playlist_videos` (D-058/D-059, schema v17/v18 postdate `FORMAT.md`) — already broken relative to its own "you can leave with everything" promise, independent of import; and `local-data.md` already sketches an import counterpart ("post-MVP but schema-stable," merge-by-`statusChangedAt`) that was never built (no `data:import` channel, no `dialog.showOpenDialog` anywhere in the app). **Resolved design:** (1) credentials never travel in the backup (D-013 — `safeStorage` is machine-tied, each user owns their own OAuth client) — restoring always still goes through the wizard's normal OAuth connect step; raised by the owner directly ("se o usuário restaurar um backup mas conectar outro usuário?") and confirmed safe by reading the code, not assumed — subscriptions/read-state/favorites/playlists are already account-agnostic in the schema (D-003, "the user's own facts, not tied to whichever account surfaced the video"), so restoring under a *different* Google account than the backup was made on just has the next sync reconcile `account_channels` against that account's real subscriptions the same way an ordinary YouTube-side unsubscribe already does (D-048 — retained, not deleted); (2) still worth a heads-up per the owner's own wording — a confirm dialog reading **"Isso foi feito em outra conta. Tem certeza que deseja prosseguir com o restore?"** with a ⓘ icon (hover for the fuller explanation), shown only when the backup's recorded account identity (`accounts[].channelId`, a new export field — the connected account's own channel id, free on the existing `getOwnChannel()` call, needs a new `accounts.channel_id` column, schema v19) doesn't match any currently-connected account; older backups with no `accounts` field skip the check silently; (3) import ships in two places sharing one backend path — the onboarding wizard (`WelcomeStep` gets a "Restaurar de um backup" entry, picked/validated before the OAuth steps, actually applied right after `ConnectStep` succeeds once the connected account's identity is known) for a fresh install, and Settings (beside the existing Export button) for merging into an install already in use. **One recommendation flagged rather than asked about directly, per this project's own "flag when a recommendation is exercised" rule:** the wizard/fresh path applies the backup's `settings` wholesale (nothing to conflict with on a fresh install); the Settings/merge path never touches `settings.json` at all, so an unrelated "restore my old playlists" action can't silently change an already-in-use install's theme/density/notify prefs. Merge conflict rules: `channels`/`videos` always upsert (freshest wins, same as ordinary sync); `video_state` merges by `video_id`, imported row wins only if its `statusChangedAt` is strictly newer (the rule `local-data.md` already sketched, now actually specified for real); `playlists`/`playlist_videos` insert only if the `playlist_id` doesn't already exist locally (idempotent — re-running the same import never duplicates); `account_channels`/subscriptions are only seeded in fresh mode, never touched in merge mode. Full plan on file at `/home/adriano/.claude/plans/idempotent-pondering-bear.md` for whenever this gets built. | `local-data.md`, `FORMAT.md`, `onboarding.md` |

## Key assumptions to verify (tracked; promote or correct on verification)

| Where | Assumption | Verify in |
|---|---|---|
| `authentication.md` | Publishing an External consent screen to production without verification still works for the sensitive-not-restricted `youtube.readonly` scope, and removes the 7-day refresh-token expiry | M2 |
| `youtube-api.md` | Quota costs table (1-unit list calls, 100-unit search, 10k/day default) is current | M2 |
| `youtube-api.md` | Channel RSS feeds remain available, ~15 items, minutes-fresh | M2 |
| `youtube-api.md` | `UC…`→`UU…` uploads-playlist transform is reliable (would save a `channels.list` pass) | M2 |
| `youtube-api.md` | API data-retention policy window (~30 days) — confirm exact current rule | M2 |
| `feed.md` | RSS `published` reflects public availability for premieres/scheduled videos | M2 |
| `feed.md` | No Shorts flag in the API; duration + `/shorts/{id}` HEAD heuristic discriminates reliably — **now MVP-blocking (D-028), verify early in M2** | M2 |
| `local-data.md` | No runtime quota-remaining API exists (client-side accounting needed) | M2 |
| `architecture.md` | SQLite WAL comfortably handles 1 writer + 1 reader at ≤100k rows | M1 |
| `decisions.md` (D-050) | Windows/macOS auto-start (`setLoginItemSettings`) and OS notifications behave as expected on those platforms, incl. whether D-043's unsigned/non-notarized macOS build degrades Notification Center registration — owner's hands-on verification is Linux-only | owner's own Windows/macOS testing |

## How to add an entry

Next free ID: **D-065**. One decision per ID. State the options, the recommendation and
its rationale (or the final choice and why), the milestone it blocks, and the owning spec.
Never edit a Final entry's meaning — supersede it with a new entry and mark the old one
**Superseded by D-NNN**.
